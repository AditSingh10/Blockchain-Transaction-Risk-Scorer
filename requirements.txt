# Core ML — torch and torch-geometric are likely already installed from training.
# Do NOT add torch-scatter, torch-sparse, etc. here; those require CUDA-specific wheel URLs.
torch>=2.2.0
torch-geometric>=2.4.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Data
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.3.0
joblib>=1.3.0

# FastAPI dep (suppresses warning even when form data isn't used)
python-multipart>=0.0.9
